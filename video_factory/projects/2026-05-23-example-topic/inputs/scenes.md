# Scene planning instructions (optional)

- Target ~3 seconds per still frame.
- One sentence or clause per scene.
- Keep the same character design and line style in every `image_prompt`.
- Avoid complex poses that break faces (profile or 3/4 view preferred).
