[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "video-factory"
version = "0.1.0"
description = "Short-video pipeline engine (script, scenes, visuals, voiceover, FFmpeg render)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "google-genai>=1.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "pyyaml>=6.0",
    "pillow>=10.0.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "ruff>=0.4.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/video_factory"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
